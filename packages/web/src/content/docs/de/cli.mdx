---
title: CLI
description: Blackbox AI CLI Optionen und Befehle.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Der Blackbox AI CLI startet standardmäßig das [TUI](/docs/tui), wenn er ohne Argumente ausgeführt wird.

```bash
blackbox ai
```

Es akzeptiert aber auch Befehle, wie auf dieser Seite dokumentiert. Dadurch können Sie programmgesteuert mit Blackbox AI interagieren.

```bash
blackbox ai run "Explain how closures work in JavaScript"
```

---

### tui

Starten Sie die Terminalbenutzeroberfläche Blackbox AI.

```bash
blackbox ai [project]
```

#### Optionen

| Flag         | Kurz | Beschreibung                                                           |
| ------------ | ---- | ---------------------------------------------------------------------- |
| `--continue` | `-c` | Setzen Sie die letzte Sitzung fort                                     |
| `--session`  | `-s` | Session-ID zum Fortfahren                                              |
| `--fork`     |      | Sitzung beim Fortsetzen verzweigen (mit `--continue` oder `--session`) |
| `--prompt`   |      | Prompt zur Verwendung                                                  |
| `--model`    | `-m` | Zu verwendendes Modell in der Form provider/model                      |
| `--agent`    |      | Zu verwendender Agent                                                  |
| `--port`     |      | Port zum Abhören                                                       |
| `--hostname` |      | Hostname zum Abhören                                                   |

---

## Befehle

Der Blackbox AI CLI verfügt außerdem über die folgenden Befehle.

---

### agent

Agenten für Blackbox AI verwalten.

```bash
blackbox ai agent [command]
```

---

### attach

Schließen Sie ein Terminal an einen bereits laufenden Blackbox AI-Backend-Server an, der über die Befehle `serve` oder `web` gestartet wurde.

```bash
blackbox ai attach [url]
```

Dies ermöglicht die Verwendung von TUI mit einem Remote-Backend Blackbox AI. Zum Beispiel:

```bash
# Start the backend server for web/mobile access
blackbox ai web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
blackbox ai attach http://10.20.30.40:4096
```

#### Optionen

| Flag        | Kurz | Beschreibung                              |
| ----------- | ---- | ----------------------------------------- |
| `--dir`     |      | Arbeitsverzeichnis zum Starten von TUI in |
| `--session` | `-s` | Session-ID zum Fortfahren                 |

---

#### create

Erstellen Sie einen neuen Agenten mit benutzerdefinierter Konfiguration.

```bash
blackbox ai agent create
```

Dieser Befehl führt Sie durch die Erstellung eines neuen Agenten mit einer benutzerdefinierten Systemeingabeaufforderung und Toolkonfiguration.

---

#### list

Listen Sie alle verfügbaren Agenten auf.

```bash
blackbox ai agent list
```

---

### auth

Befehl zum Verwalten von Anmeldeinformationen und Anmelden für Anbieter.

```bash
blackbox ai auth [command]
```

---

#### login

Blackbox AI wird von der Anbieterliste bei [Models.dev](https://models.dev) unterstützt, sodass Sie `blackbox ai auth login` verwenden können, um API-Schlüssel für jeden Anbieter zu konfigurieren, den Sie verwenden möchten. Dies wird in `~/.local/share/blackbox ai/auth.json` gespeichert.

```bash
blackbox ai auth login
```

Wenn Blackbox AI startet, lädt es den Anbieter aus der Anmeldeinformationsdatei. Und ob in Ihren Umgebungen Schlüssel sind oder in Ihrem Projekt eine `.env`-Datei vorhanden ist.

---

#### list

Listet alle authentifizierten Anbieter auf, wie in der Anmeldeinformationsdatei gespeichert.

```bash
blackbox ai auth list
```

Oder die Kurzversion.

```bash
blackbox ai auth ls
```

---

#### logout

Melden Sie sich von einem Anbieter ab, indem Sie ihn aus der Anmeldeinformationsdatei löschen.

```bash
blackbox ai auth logout
```

---

### github

Verwalten Sie den GitHub-Agenten für die Repository-Automatisierung.

```bash
blackbox ai github [command]
```

---

#### install

Installieren Sie den GitHub-Agenten in Ihrem Repository.

```bash
blackbox ai github install
```

Dadurch wird der erforderliche GitHub-Aktionsworkflow eingerichtet und Sie durch den Konfigurationsprozess geführt. [Learn more](/docs/github).

---

#### run

Führen Sie den GitHub-Agenten aus. Dies wird normalerweise in GitHub-Aktionen verwendet.

```bash
blackbox ai github run
```

##### Optionen

| Flag      | Beschreibung                                        |
| --------- | --------------------------------------------------- |
| `--event` | GitHub Scheinereignis zum Ausführen des Agenten für |
| `--token` | GitHub persönliches Zugriffstoken                   |

---

### mcp

Verwalten Sie Model Context Protocol-Server.

```bash
blackbox ai mcp [command]
```

---

#### add

Fügen Sie Ihrer Konfiguration einen MCP-Server hinzu.

```bash
blackbox ai mcp add
```

Dieser Befehl führt Sie durch das Hinzufügen eines lokalen oder Remote-MCP-Servers.

---

#### list

Listen Sie alle konfigurierten MCP-Server und deren Verbindungsstatus auf.

```bash
blackbox ai mcp list
```

Oder nutzen Sie die Kurzversion.

```bash
blackbox ai mcp ls
```

---

#### auth

Authentifizieren Sie sich mit einem OAuth-fähigen MCP-Server.

```bash
blackbox ai mcp auth [name]
```

Wenn Sie keinen Servernamen angeben, werden Sie aufgefordert, einen der verfügbaren OAuth-fähigen Server auszuwählen.

Sie können auch OAuth-fähige Server und deren Authentifizierungsstatus auflisten.

```bash
blackbox ai mcp auth list
```

Oder nutzen Sie die Kurzversion.

```bash
blackbox ai mcp auth ls
```

---

#### logout

Entfernen Sie OAuth-Anmeldeinformationen für einen MCP-Server.

```bash
blackbox ai mcp logout [name]
```

---

#### debug

Beheben Sie OAuth-Verbindungsprobleme für einen MCP-Server.

```bash
blackbox ai mcp debug <name>
```

---

### models

Listen Sie alle verfügbaren Modelle der konfigurierten Anbieter auf.

```bash
blackbox ai models [provider]
```

Dieser Befehl zeigt alle bei Ihren konfigurierten Anbietern verfügbaren Modelle im Format `provider/model` an.

Dies ist nützlich, um die genauen Modellnamen herauszufinden, die in [Ihrer Konfiguration](/docs/config/) verwendet werden sollen.

Sie können optional eine Anbieter-ID übergeben, um Modelle nach diesem Anbieter zu filtern.

```bash
blackbox ai models anthropic
```

#### Optionen

| Flag        | Beschreibung                                                                          |
| ----------- | ------------------------------------------------------------------------------------- |
| `--refresh` | Aktualisieren Sie den Modellcache von models.dev                                      |
| `--verbose` | Verwenden Sie eine ausführlichere Modellausgabe (einschließlich Metadaten wie Kosten) |

Verwenden Sie das Flag `--refresh`, um die zwischengespeicherte Modellliste zu aktualisieren. Dies ist nützlich, wenn einem Anbieter neue Modelle hinzugefügt wurden und Sie diese in Blackbox AI sehen möchten.

```bash
blackbox ai models --refresh
```

---

### run

Führen Sie Blackbox AI im nicht interaktiven Modus aus, indem Sie direkt eine Eingabeaufforderung übergeben.

```bash
blackbox ai run [message..]
```

Dies ist nützlich für die Skripterstellung, die Automatisierung oder wenn Sie eine schnelle Antwort wünschen, ohne das vollständige TUI zu starten. Zum Beispiel:

```bash "blackbox ai run"
blackbox ai run Explain the use of context in Go
```

Sie können auch eine Verbindung zu einer laufenden `blackbox ai serve`-Instanz herstellen, um bei jedem Lauf Kaltstartzeiten für den MCP-Server zu vermeiden:

```bash
# Start a headless server in one terminal
blackbox ai serve

# In another terminal, run commands that attach to it
blackbox ai run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Optionen

| Flag         | Kurz | Beschreibung                                                                                        |
| ------------ | ---- | --------------------------------------------------------------------------------------------------- |
| `--command`  |      | Der auszuführende Befehl, Argumente als Nachricht verwenden                                         |
| `--continue` | `-c` | Setzen Sie die letzte Sitzung fort                                                                  |
| `--session`  | `-s` | Session-ID zum Fortfahren                                                                           |
| `--fork`     |      | Sitzung beim Fortsetzen verzweigen (mit `--continue` oder `--session` verwenden)                    |
| `--share`    |      | Teilen Sie die Sitzung                                                                              |
| `--model`    | `-m` | Zu verwendendes Modell in der Form provider/model                                                   |
| `--agent`    |      | Zu verwendender Agent                                                                               |
| `--file`     | `-f` | Datei(en) zum Anhängen an die Nachricht                                                             |
| `--format`   |      | Format: default (formatiert) oder json (rohe JSON-Ereignisse)                                       |
| `--title`    |      | Titel für die Sitzung (verwendet eine verkürzte Eingabeaufforderung, wenn kein Wert angegeben wird) |
| `--attach`   |      | An einen laufenden Blackbox AI-Server anschließen (z.B. http://localhost:4096)                         |
| `--port`     |      | Port für den lokalen Server (standardmäßig zufälliger Port)                                         |

---

### serve

Starten Sie einen Headless-Blackbox AI-Server für den API-Zugriff. Sehen Sie sich die [Server-Dokumentation](/docs/server) für die vollständige HTTP-Schnittstelle an.

```bash
blackbox ai serve
```

Dadurch wird ein HTTP-Server gestartet, der API-Zugriff auf Blackbox AI-Funktionalität ohne die TUI-Schnittstelle bietet. Legen Sie `BLACKBOX_AI_SERVER_PASSWORD` fest, um die HTTP-Basisauthentifizierung zu aktivieren (Benutzername ist standardmäßig `blackbox ai`).

#### Optionen

| Flag         | Beschreibung                                    |
| ------------ | ----------------------------------------------- |
| `--port`     | Port zum Abhören                                |
| `--hostname` | Hostname zum Abhören                            |
| `--mdns`     | mDNS-Discovery aktivieren                       |
| `--cors`     | Zusätzliche Browser-Ursprünge für CORS zulassen |

---

### session

Blackbox AI-Sitzungen verwalten.

```bash
blackbox ai session [command]
```

---

#### list

Listen Sie alle Blackbox AI-Sitzungen auf.

```bash
blackbox ai session list
```

##### Optionen

| Flag          | Kurz | Beschreibung                             |
| ------------- | ---- | ---------------------------------------- |
| `--max-count` | `-n` | Beschränken auf die N neuesten Sitzungen |
| `--format`    |      | Ausgabeformat: table oder json (table)   |

---

### stats

Zeigen Sie Token-Nutzungs- und Kostenstatistiken für Ihre Blackbox AI-Sitzungen an.

```bash
blackbox ai stats
```

#### Optionen

| Flag        | Beschreibung                                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `--days`    | Statistiken für die letzten N Tage anzeigen (alle Zeiten)                                                                      |
| `--tools`   | Anzahl der angebotenen Werkzeuge (alle)                                                                                        |
| `--models`  | Aufschlüsselung der Modellnutzung anzeigen (standardmäßig ausgeblendet). Übergeben Sie eine Zahl, um die obersten N anzuzeigen |
| `--project` | Nach Projekt filtern (alle Projekte, leere Zeichenfolge: aktuelles Projekt)                                                    |

---

### export

Sitzungsdaten als JSON exportieren.

```bash
blackbox ai export [sessionID]
```

Wenn Sie keine Sitzungs-ID angeben, werden Sie aufgefordert, eine der verfügbaren Sitzungen auszuwählen.

---

### import

Importieren Sie Sitzungsdaten aus einer JSON-Datei oder einer Blackbox AI-Freigabe-URL.

```bash
blackbox ai import <file>
```

Sie können aus einer lokalen Datei oder einer Blackbox AI-Freigabe-URL importieren.

```bash
blackbox ai import session.json
blackbox ai import https://opncd.ai/s/abc123
```

---

### web

Starten Sie einen Headless-Blackbox AI-Server mit einer Weboberfläche.

```bash
blackbox ai web
```

Dadurch wird ein HTTP-Server gestartet und ein Webbrowser geöffnet, um über eine Webschnittstelle auf Blackbox AI zuzugreifen. Legen Sie `BLACKBOX_AI_SERVER_PASSWORD` fest, um die HTTP-Basisauthentifizierung zu aktivieren (Benutzername ist standardmäßig `blackbox ai`).

#### Optionen

| Flag         | Beschreibung                                    |
| ------------ | ----------------------------------------------- |
| `--port`     | Port zum Abhören                                |
| `--hostname` | Hostname zum Abhören                            |
| `--mdns`     | mDNS-Discovery aktivieren                       |
| `--cors`     | Zusätzliche Browser-Ursprünge für CORS zulassen |

---

### acp

Starten Sie einen ACP (Agent Client Protocol) Server.

```bash
blackbox ai acp
```

Dieser Befehl startet einen ACP-Server, der über stdin/stdout unter Verwendung von nd-JSON kommuniziert.

#### Optionen

| Flag         | Beschreibung         |
| ------------ | -------------------- |
| `--cwd`      | Arbeitsverzeichnis   |
| `--port`     | Port zum Abhören     |
| `--hostname` | Hostname zum Abhören |

---

### uninstall

Deinstallieren Sie Blackbox AI und entfernen Sie alle zugehörigen Dateien.

```bash
blackbox ai uninstall
```

#### Optionen

| Flag            | Kurz | Beschreibung                                        |
| --------------- | ---- | --------------------------------------------------- |
| `--keep-config` | `-c` | Konfigurationsdateien behalten                      |
| `--keep-data`   | `-d` | Sitzungsdaten und Snapshots aufbewahren             |
| `--dry-run`     |      | Zeigt, was entfernt werden würde, ohne zu entfernen |
| `--force`       | `-f` | Bestätigungsaufforderungen überspringen             |

---

### upgrade

Aktualisiert Blackbox AI auf die neueste Version oder eine bestimmte Version.

```bash
blackbox ai upgrade [target]
```

Um auf die neueste Version zu aktualisieren:

```bash
blackbox ai upgrade
```

Um auf eine bestimmte Version zu aktualisieren.

```bash
blackbox ai upgrade v0.1.48
```

#### Optionen

| Flag       | Kurz | Beschreibung                                                        |
| ---------- | ---- | ------------------------------------------------------------------- |
| `--method` | `-m` | Die zu verwendende Installationsmethode; curl, npm, pnpm, bun, brew |

---

## Globale Flags

Der Blackbox AI CLI akzeptiert die folgenden globalen Flags.

| Flag           | Kurz | Beschreibung                              |
| -------------- | ---- | ----------------------------------------- |
| `--help`       | `-h` | Hilfe anzeigen                            |
| `--version`    | `-v` | Versionsnummer drucken                    |
| `--print-logs` |      | Protokolle nach stderr drucken            |
| `--log-level`  |      | Protokollebene (DEBUG, INFO, WARN, ERROR) |

---

## Umgebungsvariablen

Blackbox AI kann mithilfe von Umgebungsvariablen konfiguriert werden.

| Variable                              | Typ             | Beschreibung                                                                     |
| ------------------------------------- | --------------- | -------------------------------------------------------------------------------- |
| `BLACKBOX_AI_AUTO_SHARE`                 | boolescher Wert | Sitzungen automatisch teilen                                                     |
| `BLACKBOX_AI_GIT_BASH_PATH`              | Zeichenfolge    | Pfad zur ausführbaren Git Bash-Datei unter Windows                               |
| `BLACKBOX_AI_CONFIG`                     | Zeichenfolge    | Pfad zur Konfigurationsdatei                                                     |
| `BLACKBOX_AI_CONFIG_DIR`                 | Zeichenfolge    | Pfad zum Konfigurationsverzeichnis                                               |
| `BLACKBOX_AI_CONFIG_CONTENT`             | Zeichenfolge    | Inline-JSON-Konfigurationsinhalt                                                 |
| `BLACKBOX_AI_DISABLE_AUTOUPDATE`         | boolescher Wert | Automatische Update-Prüfungen deaktivieren                                       |
| `BLACKBOX_AI_DISABLE_PRUNE`              | boolescher Wert | Bereinigung alter Daten deaktivieren                                             |
| `BLACKBOX_AI_DISABLE_TERMINAL_TITLE`     | boolescher Wert | Deaktivieren Sie die automatische Aktualisierung von Terminaltiteln              |
| `BLACKBOX_AI_PERMISSION`                 | Zeichenfolge    | Inline-JSON-Berechtigungskonfiguration                                           |
| `BLACKBOX_AI_DISABLE_DEFAULT_PLUGINS`    | boolescher Wert | Standard-Plugins deaktivieren                                                    |
| `BLACKBOX_AI_DISABLE_LSP_DOWNLOAD`       | boolescher Wert | Automatische LSP-Server-Downloads deaktivieren                                   |
| `BLACKBOX_AI_ENABLE_EXPERIMENTAL_MODELS` | boolescher Wert | Experimentelle Modelle aktivieren                                                |
| `BLACKBOX_AI_DISABLE_AUTOCOMPACT`        | boolescher Wert | Automatische Kontextkomprimierung deaktivieren                                   |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE`        | boolescher Wert | Deaktivieren Sie das Lesen von `.claude` (Prompt + Skills)                       |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_PROMPT` | boolescher Wert | Deaktivieren Sie das Lesen von `~/.claude/CLAUDE.md`                             |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_SKILLS` | boolescher Wert | Deaktivieren Sie das Laden von `.claude/skills`                                  |
| `BLACKBOX_AI_DISABLE_MODELS_FETCH`       | boolescher Wert | Deaktivieren Sie das Abrufen von Modellen aus Remote-Quellen                     |
| `BLACKBOX_AI_FAKE_VCS`                   | Zeichenfolge    | Gefälschter VCS-Anbieter zu Testzwecken                                          |
| `BLACKBOX_AI_DISABLE_FILETIME_CHECK`     | boolescher Wert | Dateizeitprüfung zur Optimierung deaktivieren                                    |
| `BLACKBOX_AI_CLIENT`                     | Zeichenfolge    | Client-ID (standardmäßig `cli`)                                                  |
| `BLACKBOX_AI_ENABLE_EXA`                 | boolescher Wert | Exa-Websuchtools aktivieren                                                      |
| `BLACKBOX_AI_SERVER_PASSWORD`            | Zeichenfolge    | Aktivieren Sie die Basisauthentifizierung für `serve`/`web`                      |
| `BLACKBOX_AI_SERVER_USERNAME`            | Zeichenfolge    | Benutzernamen für die Basisauthentifizierung überschreiben (Standard `blackbox ai`) |
| `BLACKBOX_AI_MODELS_URL`                 | Zeichenfolge    | Benutzerdefinierte URL zum Abrufen der Modellkonfiguration                       |

---

### Experimentell

Diese Umgebungsvariablen ermöglichen experimentelle Funktionen, die sich ändern oder entfernt werden können.

| Variable                                        | Typ             | Beschreibung                                            |
| ----------------------------------------------- | --------------- | ------------------------------------------------------- |
| `BLACKBOX_AI_EXPERIMENTAL`                         | boolescher Wert | Alle experimentellen Funktionen aktivieren              |
| `BLACKBOX_AI_EXPERIMENTAL_ICON_DISCOVERY`          | boolescher Wert | Symbolerkennung aktivieren                              |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolescher Wert | Kopieren bei Auswahl in TUI deaktivieren                |
| `BLACKBOX_AI_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | Zahl            | Standard-Timeout für Bash-Befehle in ms                 |
| `BLACKBOX_AI_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | Zahl            | Maximale Ausgabetokens für LLM-Antworten                |
| `BLACKBOX_AI_EXPERIMENTAL_FILEWATCHER`             | boolescher Wert | Dateiüberwachung für das gesamte Verzeichnis aktivieren |
| `BLACKBOX_AI_EXPERIMENTAL_OXFMT`                   | boolescher Wert | Oxfmt-Formatierer aktivieren                            |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TOOL`                | boolescher Wert | Experimentelles LSP-Tool aktivieren                     |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolescher Wert | Dateiüberwachung deaktivieren                           |
| `BLACKBOX_AI_EXPERIMENTAL_EXA`                     | boolescher Wert | Experimentelle Exa-Funktionen aktivieren                |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TY`                  | boolescher Wert | Experimentelle LSP-Typprüfung aktivieren                |
| `BLACKBOX_AI_EXPERIMENTAL_MARKDOWN`                | boolescher Wert | Experimentelle Markdown-Funktionen aktivieren           |
| `BLACKBOX_AI_EXPERIMENTAL_PLAN_MODE`               | boolescher Wert | Planmodus aktivieren                                    |
