---
title: CLI
description: blackbox ai CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

blackbox ai CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
blackbox ai
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, blackbox ai ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
blackbox ai run "Explain how closures work in JavaScript"
```

---

### tui

blackbox ai terminal kullanıcı arayüzünü başlatır.

```bash
blackbox ai [project]
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                    |
| ------------ | ---- | --------------------------------------------------------------------------- |
| `--continue` | `-c` | Son oturuma devam et                                                        |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                 |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| `--prompt`   |      | Kullanılacak prompt                                                         |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                 |
| `--agent`    |      | Kullanılacak agent                                                          |
| `--port`     |      | Dinlenecek port                                                             |
| `--hostname` |      | Dinlenecek host adı                                                         |

---

## Komutlar

blackbox ai CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

blackbox ai için agent'ları yönetin.

```bash
blackbox ai agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir blackbox ai arka uç sunucusuna bir terminal ekleyin.

```bash
blackbox ai attach [url]
```

Bu, TUI öğesinin uzak bir blackbox ai arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
blackbox ai web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
blackbox ai attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak      | Kısa | Tanım                                |
| ----------- | ---- | ------------------------------------ |
| `--dir`     |      | TUI'yi başlatmak için çalışma dizini |
| `--session` | `-s` | Devam edecek oturum açma bilgileri   |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
blackbox ai agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
blackbox ai agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
blackbox ai auth [command]
```

---

#### login

blackbox ai, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `blackbox ai auth login` kullanabilirsiniz. Bu `~/.local/share/blackbox ai/auth.json`'de saklanıyor.

```bash
blackbox ai auth login
```

blackbox ai başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
blackbox ai auth list
```

Veya kısa versiyonu.

```bash
blackbox ai auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
blackbox ai auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
blackbox ai github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
blackbox ai github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
blackbox ai github run
```

##### Bayraklar

| Bayrak    | Açıklama                                    |
| --------- | ------------------------------------------- |
| `--event` | Aracıyı çalıştırmak için GitHub sahte olayı |
| `--token` | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
blackbox ai mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
blackbox ai mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
blackbox ai mcp list
```

Veya kısa versiyonunu kullanın.

```bash
blackbox ai mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
blackbox ai mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
blackbox ai mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
blackbox ai mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
blackbox ai mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
blackbox ai mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
blackbox ai models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
blackbox ai models anthropic
```

#### Bayraklar

| Bayrak      | Tanım                                                                       |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Modeller.dev'den model önbelleğini yenileyin                                |
| `--verbose` | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları blackbox ai'da görmek istediğinizde kullanışlıdır.

```bash
blackbox ai models --refresh
```

---

### run

Doğrudan bir istem ileterek blackbox ai'u etkileşimli olmayan modda çalıştırın.

```bash
blackbox ai run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "blackbox ai run"
blackbox ai run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `blackbox ai serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
blackbox ai serve

# In another terminal, run commands that attach to it
blackbox ai run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------- |
| `--command`  |      | Çalıştırılacak komut, args için mesajı kullanın                                   |
| `--continue` | `-c` | Son oturuma devam et                                                              |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                       |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)       |
| `--share`    |      | Oturumu paylaşın                                                                  |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                       |
| `--agent`    |      | Kullanılacak temsilci                                                             |
| `--file`     | `-f` | Mesaja eklenecek dosya(lar)                                                       |
| `--format`   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                |
| `--title`    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)          |
| `--attach`   |      | Çalışan bir blackbox ai sunucusuna ekleyin (ör. http://localhost:4096)               |
| `--port`     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır) |

---

### serve

API erişimi için headless bir blackbox ai sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
blackbox ai serve
```

Bu, TUI arayüzü olmadan blackbox ai işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `BLACKBOX_AI_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `blackbox ai` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

blackbox ai oturumlarını yönetin.

```bash
blackbox ai session [command]
```

---

#### list

Tüm blackbox ai oturumlarını listeleyin.

```bash
blackbox ai session list
```

##### Bayraklar

| Bayrak        | Kısa | Tanım                                  |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | En son N oturumla sınırla              |
| `--format`    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

blackbox ai oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
blackbox ai stats
```

#### Bayraklar

| Bayrak      | Açıklama                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------- |
| `--days`    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| `--tools`   | Gösterilecek araç sayısı (tümü)                                                                             |
| `--models`  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| `--project` | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
blackbox ai export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya blackbox ai paylaşımından URL oturum verilerini içe aktarın.

```bash
blackbox ai import <file>
```

Yerel bir dosyadan veya blackbox ai paylaşımından (URL) içe aktarabilirsiniz.

```bash
blackbox ai import session.json
blackbox ai import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir blackbox ai sunucusu başlatın.

```bash
blackbox ai web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla blackbox ai'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `BLACKBOX_AI_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `blackbox ai` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
blackbox ai acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak       | Açıklama            |
| ------------ | ------------------- |
| `--cwd`      | Çalışma dizini      |
| `--port`     | Dinlenecek port     |
| `--hostname` | Dinlenecek host adı |

---

### uninstall

blackbox ai'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
blackbox ai uninstall
```

#### Bayraklar

| Bayrak          | Kısa | Tanım                                           |
| --------------- | ---- | ----------------------------------------------- |
| `--keep-config` | `-c` | Yapılandırma dosyalarını sakla                  |
| `--keep-data`   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| `--dry-run`     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| `--force`       | `-f` | Onay istemlerini atla                           |

---

### upgrade

blackbox ai'u en son sürüme veya belirli bir sürüme günceller.

```bash
blackbox ai upgrade [target]
```

En son sürüme yükseltmek için.

```bash
blackbox ai upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
blackbox ai upgrade v0.1.48
```

#### Bayraklar

| Bayrak     | Kısa | Açıklama                                               |
| ---------- | ---- | ------------------------------------------------------ |
| `--method` | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

blackbox ai CLI aşağıdaki global bayrakları destekler.

| Bayrak         | Kısa | Tanım                                    |
| -------------- | ---- | ---------------------------------------- |
| `--help`       | `-h` | Yardımı görüntüle                        |
| `--version`    | `-v` | Sürüm numarasını yazdır                  |
| `--print-logs` |      | Günlükleri stderr'e yazdır               |
| `--log-level`  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

blackbox ai ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `BLACKBOX_AI_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `BLACKBOX_AI_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `BLACKBOX_AI_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `BLACKBOX_AI_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `BLACKBOX_AI_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `BLACKBOX_AI_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `BLACKBOX_AI_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `BLACKBOX_AI_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `BLACKBOX_AI_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `BLACKBOX_AI_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `BLACKBOX_AI_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `BLACKBOX_AI_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `BLACKBOX_AI_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `BLACKBOX_AI_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `BLACKBOX_AI_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `BLACKBOX_AI_DISABLE_FILETIME_CHECK`     | boolean | Optimizasyon için dosya süresi kontrolünü devre dışı bırakın                |
| `BLACKBOX_AI_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `BLACKBOX_AI_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `BLACKBOX_AI_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `BLACKBOX_AI_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `blackbox ai`) |
| `BLACKBOX_AI_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `BLACKBOX_AI_EXPERIMENTAL`                         | boolean | Tüm deneysel özellikleri etkinleştir                    |
| `BLACKBOX_AI_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `BLACKBOX_AI_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `BLACKBOX_AI_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `BLACKBOX_AI_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `BLACKBOX_AI_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `BLACKBOX_AI_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TY`                  | boolean | Deneysel LSP tür denetimini etkinleştir                 |
| `BLACKBOX_AI_EXPERIMENTAL_MARKDOWN`                | boolean | Deneysel işaretleme özelliklerini etkinleştir           |
| `BLACKBOX_AI_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
