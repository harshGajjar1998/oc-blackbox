---
title: CLI
description: Blackbox AI CLI muligheder og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Blackbox AI CLI starter som standard [TUI](/docs/tui), når den kører uden nogen argumenter.

```bash
blackbox ai
```

Men den accepterer også kommandoer som dokumenteret på denne side. Dette giver dig mulighed for at interagere med Blackbox AI programmatisk.

```bash
blackbox ai run "Explain how closures work in JavaScript"
```

---

### tui

Start Blackbox AI terminalbrugergrænsefladen.

```bash
blackbox ai [project]
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                  |
| ------------ | ---- | ---------------------------------------------------------------------------- |
| `--continue` | `-c` | Fortsæt sidste session                                                       |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                 |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`) |
| `--prompt`   |      | Spørg om at bruge                                                            |
| `--model`    | `-m` | Model til brug i form af provider/model                                      |
| `--agent`    |      | Agent hos bruge                                                              |
| `--port`     |      | Port at lytte på                                                             |
| `--hostname` |      | Værtsnavn at lytte på                                                        |

---

## Kommandoer

Blackbox AI CLI har også følgende kommandoer.

---

### agent

Administrer agenter i Blackbox AI.

```bash
blackbox ai agent [command]
```

---

### attach

Tilslut en terminal til en allerede kørende Blackbox AI backend-server startet via `serve` eller `web` kommandoer.

```bash
blackbox ai attach [url]
```

Dette gør det muligt at bruge TUI med en ekstern Blackbox AI backend. For eksempel:

```bash
# Start the backend server for web/mobile access
blackbox ai web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
blackbox ai attach http://10.20.30.40:4096
```

#### Flag

| Flag        | Kort | Beskrivelse                      |
| ----------- | ---- | -------------------------------- |
| `--dir`     |      | Arbejdsmappe til at starte TUI i |
| `--session` | `-s` | Sessions-id for at fortsætte     |

---

#### create

Opret en ny agent med brugerdefineret konfiguration.

```bash
blackbox ai agent create
```

Denne kommando vil guide dig gennem oprettelse af en ny agent med en brugerdefineret systemprompt og værktøjskonfiguration.

---

#### list

Liste over alle tilgængelige agenter.

```bash
blackbox ai agent list
```

---

### auth

Kommando til at administrere legitimationsoplysninger og login for udbydere.

```bash
blackbox ai auth [command]
```

---

#### login

Blackbox AI drives af udbyderlisten på [Models.dev](https://models.dev), så du kan bruge `blackbox ai auth login` til at konfigurere API-nøgler for enhver udbyder, du vil bruge. Dette er gemt i `~/.local/share/blackbox ai/auth.json`.

```bash
blackbox ai auth login
```

Når Blackbox AI starter op, indlæser den udbyderne fra legitimationsfilen. Og hvis der er nogen nøgler defineret i dine miljøer eller en `.env` fil i dit projekt.

---

#### list

Viser alle de godkendte udbydere som gemt i legitimationsfilen.

```bash
blackbox ai auth list
```

Eller den korte version.

```bash
blackbox ai auth ls
```

---

#### logout

Logger dig ud af en udbyder ved at rydde den fra legitimationsfilen.

```bash
blackbox ai auth logout
```

---

### github

Administrator GitHub-agenten til lagerautomatisering.

```bash
blackbox ai github [command]
```

---

#### install

Installatør GitHub-agenten i dit lager.

```bash
blackbox ai github install
```

Dette opsætter det nødvendige GitHub Actions workflow og guider dig gennem konfigurationsprocessen. [Learn more](/docs/github).

---

#### run

Kør GitHub-agenten. Dette bruges typisk i GitHub Actions.

```bash
blackbox ai github run
```

##### Flag

| Flag      | Beskrivelse                                    |
| --------- | ---------------------------------------------- |
| `--event` | GitHub mock begivenhed for at køre agenten for |
| `--token` | GitHub personlig adgangstoken                  |

---

### mcp

Administrator Model Context Protocol-servere.

```bash
blackbox ai mcp [command]
```

---

#### add

Tilføj en MCP-server til din konfiguration.

```bash
blackbox ai mcp add
```

Denne kommando vil guide dig gennem tilføjelse af enten en lokal eller ekstern MCP-server.

---

#### list

Liste over alle konfigurerede MCP-servere og deres forbindelsesstatus.

```bash
blackbox ai mcp list
```

Eller brug den korte version.

```bash
blackbox ai mcp ls
```

---

#### auth

Godkendt med en OAuth-aktiveret MCP-server.

```bash
blackbox ai mcp auth [name]
```

Hvis du ikke angiver et servernavn, bliver du bedt om at vælge blandt tilgængelige OAuth-kompatible servere.

Du kan også angive OAuth-kompatible servere og deres godkendelsesstatus.

```bash
blackbox ai mcp auth list
```

Eller brug den korte version.

```bash
blackbox ai mcp auth ls
```

---

#### logout

Fjern OAuth-legitimationsoplysninger for en MCP-server.

```bash
blackbox ai mcp logout [name]
```

---

#### debug

Fejlfinding af OAuth-forbindelsesproblemer for en MCP-server.

```bash
blackbox ai mcp debug <name>
```

---

### models

Liste over alle tilgængelige modeller fra konfigurerede udbydere.

```bash
blackbox ai models [provider]
```

Denne kommando viser alle tilgængelige modeller på tværs af dine konfigurerede udbydere i formatet `provider/model`.

Dette er nyttigt til at finde ud af det nøjagtige modelnavn, der skal bruges i [your config](/docs/config/).

Du kan eventuelt videregive et udbyder-id for at filtrere modeller efter den udbyder.

```bash
blackbox ai models anthropic
```

#### Flag

| Flag        | Beskrivelse                                                             |
| ----------- | ----------------------------------------------------------------------- |
| `--refresh` | Opdater modelcachen fra models.dev                                      |
| `--verbose` | Brug mere detaljeret modeloutput (inkluderer metadata som omkostninger) |

Brug flaget `--refresh` til at opdatere den cachelagrede modelliste. Dette er nyttigt, når nye modeller er blevet tilføjet til en udbyder, og du vil se dem i Blackbox AI.

```bash
blackbox ai models --refresh
```

---

### run

Kør blackbox ai i ikke-interaktiv tilstand ved at sende en prompt direkte.

```bash
blackbox ai run [message..]
```

Dette er nyttigt til scripting, automatisering, eller når du vil have et hurtigt svar uden at starte hele TUI. F.eks.

```bash "blackbox ai run"
blackbox ai run Explain the use of context in Go
```

Du kan også vedhæfte til en kørende `blackbox ai serve`-instans for at undgå MCP serverens kolde opstartstider ved hver kørsel:

```bash
# Start a headless server in one terminal
blackbox ai serve

# In another terminal, run commands that attach to it
blackbox ai run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                         |
| ------------ | ---- | ----------------------------------------------------------------------------------- |
| `--command`  |      | Kommandoen til at køre, brug besked til args                                        |
| `--continue` | `-c` | Fortsæt sidste session                                                              |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                        |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`)        |
| `--share`    |      | Del sessionen                                                                       |
| `--model`    | `-m` | Model til brug i form af provider/model                                             |
| `--agent`    |      | Agent til brug                                                                      |
| `--file`     | `-f` | Fil(er), der skal vedhæftes til meddelelsen                                         |
| `--format`   |      | Format: standard (formateret) eller json (rå JSON hændelser)                        |
| `--title`    |      | Titel for sessionen (bruger trunkeret prompt, hvis der ikke er angivet nogen værdi) |
| `--attach`   |      | Tilslut til en kørende blackbox ai-server (f.eks. http://localhost:4096)               |
| `--port`     |      | Port til den lokale server (standard til vilkårlig port)                            |

---

### serve

Start en hovedløs Blackbox AI-server til API-adgang. Tjek [server docs](/docs/server) for den fulde HTTP-grænseflade.

```bash
blackbox ai serve
```

Dette starter en HTTP-server, der giver API-adgang til blackbox ai-funktionalitet uden TUI-grænsefladen. Indstil `BLACKBOX_AI_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `blackbox ai`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### session

Administrator Blackbox AI sessionsholder.

```bash
blackbox ai session [command]
```

---

#### list

Liste over alle Blackbox AI sessioner.

```bash
blackbox ai session list
```

##### Flag

| Flag          | Kort | Beskrivelse                            |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | Begræns til N seneste sessioner        |
| `--format`    |      | Outputformat: tabel eller json (tabel) |

---

### stats

Vis tokenbrug og omkostningsstatistikker for dine Blackbox AI-sessioner.

```bash
blackbox ai stats
```

#### Flag

| Flag        | Beskrivelse                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Vis statistik for de sidste N dage (hele tiden)                             |
| `--tools`   | Antal værktøjer, der skal vises (alle)                                      |
| `--models`  | Vis modelbrugsopdeling (skjult som standard). Send et tal for at vise top N |
| `--project` | Filtre efter projekt (alle projekter, tom streng: nuværende projekt)        |

---

### export

Eksporter sessionsdata som JSON.

```bash
blackbox ai export [sessionID]
```

Hvis du ikke angiver et sessions-id, bliver du bedt om at vælge mellem tilgængelige sessioner.

---

### import

Importer sessionsdata fra en JSON fil eller Blackbox AI del URL.

```bash
blackbox ai import <file>
```

Du kan importere fra en lokal fil eller en Blackbox AI share URL.

```bash
blackbox ai import session.json
blackbox ai import https://opncd.ai/s/abc123
```

---

### web

Start en hovedløs Blackbox AI-server med en webgrænseflade.

```bash
blackbox ai web
```

Dette starter en HTTP-server og åbner en webbrowser for at få adgang til Blackbox AI via en webgrænseflade. Indstil `BLACKBOX_AI_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `blackbox ai`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### acp

Start en ACP-server (Agent Client Protocol).

```bash
blackbox ai acp
```

Denne kommando starter en ACP-server, der kommunikerer via stdin/stdout ved hjælp af nd-JSON.

#### Flag

| Flag         | Beskrivelse           |
| ------------ | --------------------- |
| `--cwd`      | Arbejdsmappe          |
| `--port`     | Port at lytte på      |
| `--hostname` | Værtsnavn at lytte på |

---

### uninstall

Afinstaller Blackbox AI og fjern alle relaterede filer.

```bash
blackbox ai uninstall
```

#### Flag

| Flag            | Kort | Beskrivelse                                      |
| --------------- | ---- | ------------------------------------------------ |
| `--keep-config` | `-c` | Se konfigurationsfiler                           |
| `--keep-data`   | `-d` | Gem sessionsdata og snapshots                    |
| `--dry-run`     |      | Vis, hvad der ville blive fjernet uden at fjerne |
| `--force`       | `-f` | Spring bekræftelsesspørgsmål over                |

---

### upgrade

Opdaterer Blackbox AI til den nyeste version eller en specifik version.

```bash
blackbox ai upgrade [target]
```

For at opgradere til den nyeste version.

```bash
blackbox ai upgrade
```

For at opgradere til en bestemt version.

```bash
blackbox ai upgrade v0.1.48
```

#### upgrade

| Flag       | Kort | Beskrivelse                                                      |
| ---------- | ---- | ---------------------------------------------------------------- |
| `--method` | `-m` | Installationsmetoden, der blev brugt; curl, npm, pnpm, bun, brew |

---

## Globale flag

blackbox ai CLI tager følgende globale flag.

| Flag           | Kort | Beskrivelse                          |
| -------------- | ---- | ------------------------------------ |
| `--help`       | `-h` | Vis hjælp                            |
| `--version`    | `-v` | Udskriftsversionsnummer              |
| `--print-logs` |      | Udskriv logfiler til stderr          |
| `--log-level`  |      | Logniveau (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

Blackbox AI kan konfigureres ved hjælp af miljøvariabler.

| Variabel                              | Skriv   | Beskrivelse                                                           |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `BLACKBOX_AI_AUTO_SHARE`                 | boolean | Del automatisk session                                                |
| `BLACKBOX_AI_GIT_BASH_PATH`              | string  | Sti til Git Bash eksekverbar på Windows                               |
| `BLACKBOX_AI_CONFIG`                     | string  | Sti til konfigurationsfil                                             |
| `BLACKBOX_AI_CONFIG_DIR`                 | string  | Sti til konfigurationsmappe                                           |
| `BLACKBOX_AI_CONFIG_CONTENT`             | string  | Indbygget json-konfigurationsindhold                                  |
| `BLACKBOX_AI_DISABLE_AUTOUPDATE`         | boolean | Deaktiver automatisk opdateringskontrol                               |
| `BLACKBOX_AI_DISABLE_PRUNE`              | boolean | Deaktiver beskæring af gamle data                                     |
| `BLACKBOX_AI_DISABLE_TERMINAL_TITLE`     | boolean | Deaktiver automatisk opdatering af terminaltitel                      |
| `BLACKBOX_AI_PERMISSION`                 | string  | Indbygget json-tilladelseskonfiguration                               |
| `BLACKBOX_AI_DISABLE_DEFAULT_PLUGINS`    | boolean | Deaktiver standard plugins                                            |
| `BLACKBOX_AI_DISABLE_LSP_DOWNLOAD`       | boolean | Deaktiver automatisk LSP-serverdownloads                              |
| `BLACKBOX_AI_ENABLE_EXPERIMENTAL_MODELS` | boolean | Aktive eksperimentelle modeller                                       |
| `BLACKBOX_AI_DISABLE_AUTOCOMPACT`        | boolean | Deaktiver automatisk kontekstkomprimering                             |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE`        | boolean | Deaktiver læsning fra `.claude` (prompt + færdigheder)                |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Deaktiver læsning `~/.claude/CLAUDE.md`                               |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Deaktiver indlæsning af `.claude/skills`                              |
| `BLACKBOX_AI_DISABLE_MODELS_FETCH`       | boolean | Deaktivering af modeller fra eksterne kilder                          |
| `BLACKBOX_AI_FAKE_VCS`                   | string  | Falsk VCS-udbyder til testformål                                      |
| `BLACKBOX_AI_DISABLE_FILETIME_CHECK`     | boolean | Deaktiver filtidskontrol for optimering                               |
| `BLACKBOX_AI_CLIENT`                     | string  | Klient-id (standard til `cli`)                                        |
| `BLACKBOX_AI_ENABLE_EXA`                 | boolean | Aktiver Exa-websøgeværktøjer                                          |
| `BLACKBOX_AI_SERVER_PASSWORD`            | string  | Aktiver grundlæggende godkendelse for `serve`/`web`                   |
| `BLACKBOX_AI_SERVER_USERNAME`            | string  | Tilsidesæt grundlæggende godkendelsesbrugernavn (standard `blackbox ai`) |
| `BLACKBOX_AI_MODELS_URL`                 | string  | Brugerdefineret URL til hentning af modelkonfiguration                |

---

### Flag

Disse miljøvariabler muliggør eksperimentelle funktioner, der kan ændres eller fjernes.

| Variabel                                        | Skriv   | Beskrivelse                                |
| ----------------------------------------------- | ------- | ------------------------------------------ |
| `BLACKBOX_AI_EXPERIMENTAL`                         | boolean | Aktiver alle eksperimentelle funktioner    |
| `BLACKBOX_AI_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Aktiver ikonopdagelse                      |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Deaktiver kopi ved valg i TUI              |
| `BLACKBOX_AI_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | nummer  | Standard timeout for bash-kommandoer i ms  |
| `BLACKBOX_AI_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | nummer  | Maks. output-tokens for LLM-svar           |
| `BLACKBOX_AI_EXPERIMENTAL_FILEWATCHER`             | boolean | Aktiver filovervågning for hele dir        |
| `BLACKBOX_AI_EXPERIMENTAL_OXFMT`                   | boolean | Aktiver oxfmt formatter                    |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TOOL`                | boolean | Aktive eksperimenter LSP værktøj           |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Deaktiver filovervågning                   |
| `BLACKBOX_AI_EXPERIMENTAL_EXA`                     | boolean | Aktive eksperimenter Exa-funktioner        |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TY`                  | boolean | Aktive forsøg LSP typekontrol              |
| `BLACKBOX_AI_EXPERIMENTAL_MARKDOWN`                | boolean | Aktive eksperimentelle markdown-funktioner |
| `BLACKBOX_AI_EXPERIMENTAL_PLAN_MODE`               | boolean | Aktiver plantilstand                       |
