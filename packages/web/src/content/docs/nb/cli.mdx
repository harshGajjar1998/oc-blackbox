---
title: CLI
description: Blackbox AI CLI-alternativer og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Blackbox AI CLI starter som standard [TUI](/docs/tui) når den kjøres uten noen argumenter.

```bash
blackbox ai
```

Men den godtar også kommandoer som dokumentert på denne siden. Dette lar deg samhandle med Blackbox AI programmatisk.

```bash
blackbox ai run "Explain how closures work in JavaScript"
```

---

### tui

Start Blackbox AI-terminalbrukergrensesnittet.

```bash
blackbox ai [project]
```

#### Flagg

| Flagg        | Kort | Beskrivelse                                                              |
| ------------ | ---- | ------------------------------------------------------------------------ |
| `--continue` | `-c` | Fortsett siste økt                                                       |
| `--session`  | `-s` | Økt ID for å fortsette                                                   |
| `--fork`     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| `--prompt`   |      | Ledetekst som skal brukes                                                |
| `--model`    | `-m` | Modell å bruke i form av leverandør/modell                               |
| `--agent`    |      | Agent som skal brukes                                                    |
| `--port`     |      | Port å lytte på                                                          |
| `--hostname` |      | Vertsnavn å lytte på                                                     |

---

## Kommandoer

Blackbox AI CLI har også følgende kommandoer.

---

### agent

Administrer agenter for Blackbox AI.

```bash
blackbox ai agent [command]
```

---

### attach

Koble en terminal til en allerede kjørende Blackbox AI-backend-server startet via `serve` eller `web` kommandoer.

```bash
blackbox ai attach [url]
```

Dette gjør det mulig å bruke TUI med en ekstern Blackbox AI-backend. For eksempel:

```bash
# Start the backend server for web/mobile access
blackbox ai web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
blackbox ai attach http://10.20.30.40:4096
```

#### Flagg

| Flagg       | Kort | Beskrivelse                       |
| ----------- | ---- | --------------------------------- |
| `--dir`     |      | Arbeidskatalog for å starte TUI i |
| `--session` | `-s` | Økt ID for å fortsette            |

---

#### create

Opprett en ny agent med tilpasset konfigurasjon.

```bash
blackbox ai agent create
```

Denne kommandoen vil veilede deg gjennom å lage en ny agent med en tilpasset systemmelding og verktøykonfigurasjon.

---

#### list

List opp alle tilgjengelige agenter.

```bash
blackbox ai agent list
```

---

### auth

Kommando for administrasjon av påloggingsinformasjon og innlogging for leverandører.

```bash
blackbox ai auth [command]
```

---

#### login

Blackbox AI drives av leverandørlisten på [Models.dev](https://models.dev), så du kan bruke `blackbox ai auth login` til å konfigurere API-nøkler for enhver leverandør du vil bruke. Dette er lagret i `~/.local/share/blackbox ai/auth.json`.

```bash
blackbox ai auth login
```

Når Blackbox AI starter opp, laster den leverandørene fra filen med påloggingsinformasjon. Og hvis det er noen nøkler definert i miljøene dine eller en `.env`-fil i prosjektet ditt.

---

#### list

Viser alle de autentiserte leverandørene som er lagret i filen med påloggingsinformasjon.

```bash
blackbox ai auth list
```

Eller bruk kortversjonen.

```bash
blackbox ai auth ls
```

---

#### logout

Logger deg ut av en leverandør ved å fjerne den fra filen med påloggingsinformasjon.

```bash
blackbox ai auth logout
```

---

### github

Administrer GitHub-agenten for depotautomatisering.

```bash
blackbox ai github [command]
```

---

#### install

Installer GitHub-agenten i depotet ditt.

```bash
blackbox ai github install
```

Dette setter opp den nødvendige GitHub Actions-arbeidsflyten og veileder deg gjennom konfigurasjonsprosessen. [Finn ut mer](/docs/github).

---

#### run

Kjør GitHub-agenten. Dette brukes vanligvis i GitHub Actions.

```bash
blackbox ai github run
```

##### Flagg

| Flagg     | Beskrivelse                                  |
| --------- | -------------------------------------------- |
| `--event` | GitHub mock-hendelse agenten skal kjøres for |
| `--token` | GitHub personlig tilgangsnøkkel              |

---

### mcp

Administrer Model Context Protocol-servere.

```bash
blackbox ai mcp [command]
```

---

#### add

Legg til en MCP-server til konfigurasjonen.

```bash
blackbox ai mcp add
```

Denne kommandoen vil veilede deg gjennom å legge til enten en lokal eller ekstern MCP-server.

---

#### list

Liste alle konfigurerte MCP-servere og deres tilkoblingsstatus.

```bash
blackbox ai mcp list
```

Eller bruk den korte versjonen.

```bash
blackbox ai mcp ls
```

---

#### auth

Autentiser med en OAuth-aktivert MCP-server.

```bash
blackbox ai mcp auth [name]
```

Hvis du ikke oppgir et servernavn, blir du bedt om å velge blant tilgjengelige OAuth-kompatible servere.

Du kan også liste opp OAuth-kompatible servere og deres autentiseringsstatus.

```bash
blackbox ai mcp auth list
```

Eller bruk den korte versjonen.

```bash
blackbox ai mcp auth ls
```

---

#### logout

Fjern OAuth-påloggingsinformasjon for en MCP-server.

```bash
blackbox ai mcp logout [name]
```

---

#### debug

Feilsøk OAuth-tilkoblingsproblemer for en MCP-server.

```bash
blackbox ai mcp debug <name>
```

---

### models

List opp alle tilgjengelige modeller fra konfigurerte leverandører.

```bash
blackbox ai models [provider]
```

Denne kommandoen viser alle modeller som er tilgjengelige på tvers av dine konfigurerte leverandører i formatet `provider/model`.

Dette er nyttig for å finne ut det eksakte modellnavnet som skal brukes i [din konfigurasjon](/docs/config/).

Du kan eventuelt sende en leverandør ID for å filtrere modeller etter den leverandøren.

```bash
blackbox ai models anthropic
```

#### Flagg

| Flagg       | Beskrivelse                                                         |
| ----------- | ------------------------------------------------------------------- |
| `--refresh` | Oppdater modellbufferen fra models.dev                              |
| `--verbose` | Bruk mer detaljert modellutdata (inkluderer metadata som kostnader) |

Bruk `--refresh`-flagget for å oppdatere den bufrede modelllisten. Dette er nyttig når nye modeller er lagt til en leverandør og du vil se dem i Blackbox AI.

```bash
blackbox ai models --refresh
```

---

### run

Kjør Blackbox AI i ikke-interaktiv modus ved å sende en melding direkte.

```bash
blackbox ai run [message..]
```

Dette er nyttig for skripting, automatisering eller når du vil ha et raskt svar uten å starte hele TUI. For eksempel.

```bash "blackbox ai run"
blackbox ai run Explain the use of context in Go
```

Du kan også koble til en kjørende `blackbox ai serve`-forekomst for å unngå MCP-serveres kaldstarttid ved hver kjøring:

```bash
# Start a headless server in one terminal
blackbox ai serve

# In another terminal, run commands that attach to it
blackbox ai run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagg

| Flagg        | Kort | Beskrivelse                                                              |
| ------------ | ---- | ------------------------------------------------------------------------ |
| `--command`  |      | Kommandoen for å kjøre, bruk melding for args                            |
| `--continue` | `-c` | Fortsett siste økt                                                       |
| `--session`  | `-s` | Økt ID for å fortsette                                                   |
| `--fork`     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| `--share`    |      | Del økten                                                                |
| `--model`    | `-m` | Modell å bruke i form av leverandør/modell                               |
| `--agent`    |      | Agent å bruke                                                            |
| `--file`     | `-f` | Fil(er) som skal legges ved meldingen                                    |
| `--format`   |      | Format: standard (formatert) eller json (rå JSON hendelser)              |
| `--title`    |      | Tittel for økten (bruker avkortet ledetekst hvis ingen verdi er oppgitt) |
| `--attach`   |      | Koble til en kjørende Blackbox AI-server (f.eks. http://localhost:4096)     |
| `--port`     |      | Port for den lokale serveren (standard til tilfeldig port)               |

---

### serve

Start en headless Blackbox AI-server for API-tilgang. Sjekk ut [server-dokumentene](/docs/server) for hele HTTP-grensesnittet.

```bash
blackbox ai serve
```

Dette starter en HTTP-server som gir API tilgang til Blackbox AI-funksjonalitet uten TUI-grensesnittet. Sett `BLACKBOX_AI_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `blackbox ai`).

#### Flagg

| Flagg        | Beskrivelse                                        |
| ------------ | -------------------------------------------------- |
| `--port`     | Port å lytte på                                    |
| `--hostname` | Vertsnavn å lytte på                               |
| `--mdns`     | Aktiver mDNS-oppdagelse                            |
| `--cors`     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### session

Administrer Blackbox AI-økter.

```bash
blackbox ai session [command]
```

---

#### list

List opp alle Blackbox AI-økter.

```bash
blackbox ai session list
```

##### Flagg

| Flagg         | Kort | Beskrivelse                              |
| ------------- | ---- | ---------------------------------------- |
| `--max-count` | `-n` | Begrens til N siste økter                |
| `--format`    |      | Utdataformat: tabell eller json (tabell) |

---

### stats

Vis tokenbruk og kostnadsstatistikk for Blackbox AI-øktene dine.

```bash
blackbox ai stats
```

#### Flagg

| Flagg       | Beskrivelse                                                                      |
| ----------- | -------------------------------------------------------------------------------- |
| `--days`    | Vis statistikk for de siste N dagene (hele tiden)                                |
| `--tools`   | Antall verktøy som skal vises (alle)                                             |
| `--models`  | Vis oversikt over modellbruk (skjult som standard). Gi et tall for å vise topp N |
| `--project` | Filtrer etter prosjekt (alle prosjekter, tom streng: gjeldende prosjekt)         |

---

### export

Eksporter øktdata som JSON.

```bash
blackbox ai export [sessionID]
```

Hvis du ikke gir en økt ID, blir du bedt om å velge fra tilgjengelige økter.

---

### import

Importer øktdata fra en JSON-fil eller en Blackbox AI share-URL.

```bash
blackbox ai import <file>
```

Du kan importere fra en lokal fil eller en Blackbox AI share-URL.

```bash
blackbox ai import session.json
blackbox ai import https://opncd.ai/s/abc123
```

---

### web

Start en headless Blackbox AI-server med et webgrensesnitt.

```bash
blackbox ai web
```

Dette starter en HTTP-server og åpner en nettleser for å få tilgang til Blackbox AI via et nettgrensesnitt. Sett `BLACKBOX_AI_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `blackbox ai`).

#### Flagg

| Flagg        | Beskrivelse                                        |
| ------------ | -------------------------------------------------- |
| `--port`     | Port å lytte på                                    |
| `--hostname` | Vertsnavn å lytte på                               |
| `--mdns`     | Aktiver mDNS-oppdagelse                            |
| `--cors`     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### acp

Start en ACP (Agent Client Protocol) server.

```bash
blackbox ai acp
```

Denne kommandoen starter en ACP-server som kommuniserer via stdin/stdout ved å bruke nd-JSON.

#### Flagg

| Flagg        | Beskrivelse          |
| ------------ | -------------------- |
| `--cwd`      | Arbeidskatalog       |
| `--port`     | Port å lytte på      |
| `--hostname` | Vertsnavn å lytte på |

---

### uninstall

Avinstaller Blackbox AI og fjern alle relaterte filer.

```bash
blackbox ai uninstall
```

#### Flagg

| Flagg           | Kort | Beskrivelse                                   |
| --------------- | ---- | --------------------------------------------- |
| `--keep-config` | `-c` | Behold konfigurasjonsfiler                    |
| `--keep-data`   | `-d` | Behold øktdata og øyeblikksbilder             |
| `--dry-run`     |      | Vis hva som ville blitt fjernet uten å fjerne |
| `--force`       | `-f` | Hopp over bekreftelsesforespørsler            |

---

### upgrade

Oppdaterer Blackbox AI til siste versjon eller en spesifikk versjon.

```bash
blackbox ai upgrade [target]
```

For å oppgradere til siste versjon.

```bash
blackbox ai upgrade
```

For å oppgradere til en bestemt versjon.

```bash
blackbox ai upgrade v0.1.48
```

#### Flagg

| Flagg      | Kort | Beskrivelse                                                    |
| ---------- | ---- | -------------------------------------------------------------- |
| `--method` | `-m` | Installasjonsmetoden som ble brukt: curl, npm, pnpm, bun, brew |

---

## Globale flagg

Blackbox AI CLI bruker følgende globale flagg.

| Flagg          | Kort | Beskrivelse                         |
| -------------- | ---- | ----------------------------------- |
| `--help`       | `-h` | Vis hjelp                           |
| `--version`    | `-v` | Skriv ut versjonsnummer             |
| `--print-logs` |      | Skriv ut logger til stderr          |
| `--log-level`  |      | Loggnivå (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

Blackbox AI kan konfigureres ved hjelp av miljøvariabler.

| Variabel                              | Type   | Beskrivelse                                                           |
| ------------------------------------- | ------ | --------------------------------------------------------------------- |
| `BLACKBOX_AI_AUTO_SHARE`                 | boolsk | Del økter automatisk                                                  |
| `BLACKBOX_AI_GIT_BASH_PATH`              | streng | Bane til Git Bash-kjørbar på Windows                                  |
| `BLACKBOX_AI_CONFIG`                     | streng | Bane til konfigurasjonsfil                                            |
| `BLACKBOX_AI_CONFIG_DIR`                 | streng | Bane til konfigurasjonskatalog                                        |
| `BLACKBOX_AI_CONFIG_CONTENT`             | streng | Innebygd json-konfigurasjonsinnhold                                   |
| `BLACKBOX_AI_DISABLE_AUTOUPDATE`         | boolsk | Deaktiver automatiske oppdateringskontroller                          |
| `BLACKBOX_AI_DISABLE_PRUNE`              | boolsk | Deaktiver beskjæring av gamle data                                    |
| `BLACKBOX_AI_DISABLE_TERMINAL_TITLE`     | boolsk | Deaktiver automatiske terminaltitteloppdateringer                     |
| `BLACKBOX_AI_PERMISSION`                 | streng | Innebygd json-tillatelseskonfigurasjon                                |
| `BLACKBOX_AI_DISABLE_DEFAULT_PLUGINS`    | boolsk | Deaktiver standard plugins                                            |
| `BLACKBOX_AI_DISABLE_LSP_DOWNLOAD`       | boolsk | Deaktiver automatiske LSP servernedlastinger                          |
| `BLACKBOX_AI_ENABLE_EXPERIMENTAL_MODELS` | boolsk | Aktiver eksperimentelle modeller                                      |
| `BLACKBOX_AI_DISABLE_AUTOCOMPACT`        | boolsk | Deaktiver automatisk kontekstkomprimering                             |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE`        | boolsk | Deaktiver lesing fra `.claude` (spørsmål + ferdigheter)               |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_PROMPT` | boolsk | Deaktiver lesing `~/.claude/CLAUDE.md`                                |
| `BLACKBOX_AI_DISABLE_CLAUDE_CODE_SKILLS` | boolsk | Deaktiver innlasting av `.claude/skills`                              |
| `BLACKBOX_AI_DISABLE_MODELS_FETCH`       | boolsk | Deaktiver henting av modeller fra eksterne kilder                     |
| `BLACKBOX_AI_FAKE_VCS`                   | streng | Falsk VCS-leverandør for testformål                                   |
| `BLACKBOX_AI_DISABLE_FILETIME_CHECK`     | boolsk | Deaktiver filtidskontroll for optimalisering                          |
| `BLACKBOX_AI_CLIENT`                     | streng | Klientidentifikator (standard til `cli`)                              |
| `BLACKBOX_AI_ENABLE_EXA`                 | boolsk | Aktiver Exa-nettsøkeverktøy                                           |
| `BLACKBOX_AI_SERVER_PASSWORD`            | streng | Aktiver grunnleggende autentisering for `serve`/`web`                 |
| `BLACKBOX_AI_SERVER_USERNAME`            | streng | Overstyr grunnleggende autentiseringsbrukernavn (standard `blackbox ai`) |
| `BLACKBOX_AI_MODELS_URL`                 | streng | Egendefinert URL for henting av modellkonfigurasjon                   |

---

### Eksperimentelt

Disse miljøvariablene muliggjør eksperimentelle funksjoner som kan endres eller fjernes.

| Variabel                                        | Type   | Beskrivelse                                   |
| ----------------------------------------------- | ------ | --------------------------------------------- |
| `BLACKBOX_AI_EXPERIMENTAL`                         | boolsk | Aktiver alle eksperimentelle funksjoner       |
| `BLACKBOX_AI_EXPERIMENTAL_ICON_DISCOVERY`          | boolsk | Aktiver ikonoppdagelse                        |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolsk | Deaktiver kopi ved valg i TUI                 |
| `BLACKBOX_AI_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | tall   | Standard tidsavbrudd for bash-kommandoer i ms |
| `BLACKBOX_AI_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | tall   | Maks Output Tokens for LLM-svar               |
| `BLACKBOX_AI_EXPERIMENTAL_FILEWATCHER`             | boolsk | Aktiver filovervåker for hele dir             |
| `BLACKBOX_AI_EXPERIMENTAL_OXFMT`                   | boolsk | Aktiver oxfmt formatter                       |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TOOL`                | boolsk | Aktiver eksperimentelt LSP-verktøy            |
| `BLACKBOX_AI_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolsk | Deaktiver filovervåking                       |
| `BLACKBOX_AI_EXPERIMENTAL_EXA`                     | boolsk | Aktiver eksperimentelle Exa-funksjoner        |
| `BLACKBOX_AI_EXPERIMENTAL_LSP_TY`                  | boolsk | Aktiver eksperimentell LSP-typekontroll       |
| `BLACKBOX_AI_EXPERIMENTAL_MARKDOWN`                | boolsk | Aktiver eksperimentelle Markdown-funksjoner   |
| `BLACKBOX_AI_EXPERIMENTAL_PLAN_MODE`               | boolsk | Aktiver planmodus                             |
